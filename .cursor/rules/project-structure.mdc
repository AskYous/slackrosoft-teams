---
description: 
globs: 
alwaysApply: true
---
# Project Structure

This is a React/TypeScript project built with Vite.

-   **Source Code (`src/`)**: Contains the main application code.
    -   `src/main.tsx`: The main entry point for the React application ([src/main.tsx](mdc:src/main.tsx)).
    -   `src/App.tsx`: The root React component ([src/App.tsx](mdc:src/App.tsx)).
    -   `src/components/`: Reusable UI components.
    -   `src/pages/`: Top-level page components.
    -   `src/lib/`: Utility functions and libraries specific to the project.
    -   `src/providers/`: React Context providers.
    -   `src/utils/`: General utility functions.
    -   `src/hooks/`: Custom React hooks.
    -   `src/services/`: API service integrations.
    -   `src/config/`: Application configuration files.
    -   `src/types/`: TypeScript type definitions.
    -   `src/assets/`: Static assets like images or fonts used within the source code.
    -   `src/index.css`: Global CSS styles ([src/index.css](mdc:src/index.css)).
-   **Public Assets (`public/`)**: Static files served directly by the web server (e.g., `favicon.ico`).
-   **Configuration**: Project configuration files are located in the root directory.
